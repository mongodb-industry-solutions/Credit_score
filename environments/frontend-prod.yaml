env:
  NODE_ENV: prod
  INTERNAL_API_URL: http://credit-scoring-web-app:80

# Note: NEXT_PUBLIC_API_URL is set at build time via Drone build_args
# No envSecrets needed for frontend - remove incorrect secret references

serviceAccount:
  enabled: true
  irsa:
    accountId: "275662791714"
    roleName: "kanopy-prod-cicd-irsa"